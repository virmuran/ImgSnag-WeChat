<!-- markdownlint-disable -->

<div align="center">

<img alt="ImgSnag" src="ImgSnag.png" width="128" height="128" />

# ImgSnag · 微信公众号版

<br>
<div>
    <img alt="Python" src="https://img.shields.io/badge/Python-3.13-%233776AB?logo=python">
    <img alt="platform" src="https://img.shields.io/badge/platform-Windows-lightgrey">
    <img alt="deps" src="https://img.shields.io/badge/%E4%BE%9D%E8%B5%96-requests%20%2B%20PySide6-Essentials-blue">
</div>
<div>
    <img alt="version" src="https://img.shields.io/badge/version-1.8.2-green">
    <img alt="stars" src="https://img.shields.io/github/stars/virmuran/ImgSnag-WeChat?style=social">
</div>
<br>

**一键提取微信公众号文章正文图片，批量下载无水印原图**

只为微信公众号做一件事：纯 requests 抓取，不需要浏览器内核，安装包 30 MB

</div>

## 为什么只支持微信公众号

这不是能力不足，是刻意的取舍。

早期的 ImgSnag 是通用网页图片下载器，支持微信公众号 / 抖音 / 微博 / 森空岛 / 小黑盒 / 堆糖等平台。问题出在**抗风险能力**上：

- **抖音**：一次改版就让提取策略彻底失效，维护一份随时可能废掉的适配代码，投入产出比极低
- **森空岛、微博**：图片在服务器端就压了水印，抓下来也是一张带水印的图，等于没抓到
- **其他站点**：流量小、结构多变，为了少数需求维护多套策略，最后每套都是半成品

而微信公众号结构多年稳定，文章是静态 HTML（不需要 JS 渲染），正文本图本身不带水印。于是这个项目收缩到**只做微信公众号**：

| | 旧版通用 ImgSnag | 本版 |
|---|---|---|
| 支持平台 | 7 个 | 微信公众号 |
| 依赖 | PySide6 + requests + playwright | PySide6-Essentials + requests |
| 需下载浏览器内核 | 是（Chromium ≈ 300 MB） | 否 |
| 安装包 | 592 MB（解压后） | 29.1 MB（安装包）/ 30.4 MB（便携包） |

少即是多——单一平台可以做得足够深，也不怕它哪天变。

## 功能特性

- 📐 **原图画质（默认开启）**：微信同一张图提供多个画质档位，正文 HTML 里给的通常是压缩档（地址带 `/640`），程序自动改写成原图档（`/0`）再下载。实测同一张图 `1080×1423 / 101.6 KB` → `1280×1687 / 128.8 KB`，大图差距更明显；原图取不到时自动回退压缩档，不会整张失败
- 🎯 **三种图源自动叠加**：`<img data-src>` 懒加载图、`picture_page_info_list` JS 变量里的**无水印原图**、旧文章的 `<img src>`，哪种结构都能覆盖
- 🔢 **导出顺序 = 正文顺序**：导出名是 `img_01.jpg` 起的连号，排序错下图集就得手动重排。程序合并「ppi 数组顺序」与「正文标签出现顺序」定序（两来源覆盖面不同，取舍见 `extractor._merge_order`）
- 🛡 **同名不覆盖**：保存目录已有同名文件时自动加 `_1` / `_2` 后缀，连下多轮不会丢文件
- 🔍 **补充 Script 扫描**：个别文章把图片藏在 `<script>` 里，解析到 0 张图时勾选此项再解析一次即可兜底
- 🧹 **CDN 变体智能合并**：同一张图微信会从多个 CDN 节点、多个尺寸发出（`mmbiz_jpg` / `sz_mmbiz_jpg` / `/0` / `/640`），按图片身份去重，不会出现一堆重复图
- 👤 **头像与装饰自动排除**：识别作者头像字段与微信 UI 装饰图，再用内容哈希滤掉重复图
- 🚫 **右键屏蔽尺寸（可学习、可撤销）**：遇到漏网的噪音图，右键「屏蔽此尺寸」，写入配置后**下次解析自动过滤**，还会自动归类为封面 / 头像 / UI 装饰。屏蔽过的尺寸在「历史」页底部列着，带命中次数，随时能逐条恢复或全部恢复（旧版是单向门——误屏蔽一次那类图就再也回不来，只能去手改 JSON）
- 🖼 **大图预览（可缩放可拖动）+ 缩略图条**：左侧看大图，Ctrl+滚轮缩放（**以鼠标位置为中心**，想看哪放大哪）、双击在"适应窗口 / 100%"间切换，能放大到 1:1 确认真原图画质；放大超出窗口后**直接按住左键拖动**平移（图超出视口时光标即为手形；空格 + 拖动、中键拖动也可用）；右侧勾选，支持按名称 / 文件大小 / 分辨率排序
- 🔄 **统一另存格式**：原格式 / JPG / PNG / WebP 任选，一键转好再落盘
- 🗂 **每次下载一个专属文件夹（图库）**：点「下载」不再弹目录选择框 —— 图自动存进 `图片\ImgSnagWeChat\<日期_时分秒_文章标题>\`，比如 `2026-09-18_164633_秋天的第一杯奶茶`。**特意不放「下载」里**：那是最常被顺手清空的目录，图集躺在里面迟早跟着被删；而且以前平铺命名（`img_01`）连下两篇就变成 `img_01_1`，也看不出哪张属于哪篇。偶尔要放别处点「另存到…」，想看看图存哪了点「打开图库」（都在下载按钮左边）
- 👀 **历史里的图能直接在软件里翻看**：点历史记录的「查看」（或双击那一行），这一批图就从磁盘读回来进缩略图条 + 大图预览，缩放/拖动/排序/筛选都照旧 —— 不用再跳资源管理器自己找。此时是**只读浏览**：图已经在本地了，再点「下载」只会堆一份副本，所以下载按钮会禁用并说明原因，要复制到别处用「另存到…」
- 📝 **本地下载历史**：SQLite 记录每次解析与下载，可一键「再次解析」（点一下直接开解析）、在软件里「查看」、打开保存目录、删除记录；本地文件夹被清理/搬走时会标注「本地已删除」并把「查看」「打开」一并置灰（而不是点了没反应）
- 💾 **界面记事**：窗口大小位置、排序方式、保存格式、两个过滤开关、上次保存的目录都会记住，下次打开还是你上次的样子，不用每次重设一遍
- 🔔 **关于与检查更新**：点侧边栏底部的版本号打开「关于」（版本、源码地址、数据位置、第三方依赖），里面可以直接「检查更新」。平时启动后会**静默查一次**（最多 6 小时一次；发现新版本只在侧边栏亮个提示，不弹窗打扰），也可以在「关于」里把自动检查关掉
- 🔒 **数据只在你自己的电脑上**：不收集任何信息、不上报任何数据。唯一的对外请求是「检查更新」，可关闭

<!-- markdownlint-disable -->

<details><summary>点我看截图</summary>

<p align="center">
  <em>截图待补充 — 欢迎提交 PR！</em>
</p>

</details>

<!-- markdownlint-restore -->

## 下载与安装

> 本仓库只提交**源码与打包配置**。发行产物（安装包 / 便携包）请前往 [Releases](https://github.com/virmuran/ImgSnag-WeChat/releases)。
> 若还没有可用发行版，按下面「从源码运行」即可。

| 文件 | 适合谁 | 说明 |
|---|---|---|
| `ImgSnagWeChat_X.X.X_setup.exe` | 普通用户 | 安装向导（简体中文），下一步装完，开始菜单 / 桌面快捷方式、控制面板卸载 |
| `ImgSnagWeChat_X.X.X_portable.zip` | 便携党 | 解压即用、免安装、无需管理员权限，适合 U 盘或受管控的公司电脑 |

两种发行都不需要你安装 Python。**用户数据保存在用户自己的账户下**（见下表；界面偏好落在注册表），安装版卸载**不会**删除你的下载历史与过滤配置。

用户数据位置：

| 内容 | 位置 |
|---|---|
| **下载的图片（图库）** | `图片\ImgSnagWeChat\<日期_时分秒_文章标题>\`（系统「图片」目录下） |
| 下载历史（SQLite） | `~/.imgsnag_wechat/history.db` |
| 屏蔽尺寸配置 | `%APPDATA%\ImgSnagWeChat\blocked_sizes.json` |
| 界面偏好（窗口大小、排序、图库位置等） | 注册表 `HKCU\Software\ImgSnagWeChat` |

> 图库目录就是普通文件夹，随时可以整体搬走或备份 —— 但搬走之后历史里的「打开」会标成「本地已删除」，把它放回原处即可恢复。

## 从源码运行

```bash
git clone https://github.com/virmuran/ImgSnag-WeChat.git
cd ImgSnag-WeChat

python -m venv .venv
.venv\Scripts\pip install -r requirements.txt     # Windows
.venv\Scripts\python main.py
```

需要 Python 3.11+。依赖只有两个：`PySide6-Essentials`（Qt 基础模块）和 `requests`。

> 为什么不是 `PySide6`？完整元包会连带 `PySide6-Addons`（Qt3D / QtMultimedia / QtWebEngine 等）一起下载，本项目一个都用不到。实测换用 Essentials 后，装依赖从「十几分钟还没下完」变成 **36 秒**。

## 使用说明

### 1. 解析

把**公众号文章链接**（`https://mp.weixin.qq.com/s/...`）粘进输入框，点「解析图片」或按 `Ctrl+Enter`。粘贴的内容里混着分享文字也没关系，会自动把链接揪出来。

解析不了的少数情况，可以改用 HTML 源码模式：浏览器按 `F12` → 元素面板 → 右键 `<html>` → 复制 → **复制 outerHTML**，然后粘进同一个输入框点「解析图片」。点「粘贴链接」按钮会直接读取剪贴板内容。

### 2. 挑选

- **原图画质**（默认开启）：请求原图档（`/0`）而不是文章里的压缩档（`/640`）。解析时状态栏会显示本次合计体积；原图取不到会自动回退压缩档
- **过滤小图与装饰**（默认开启）：自动隐藏三类「不像正文图」的图 —— 最长边 ≤ 200px 的小图、小于 10KB 的透明 PNG、以及你右键标记过的尺寸。隐藏只是不显示、不勾选，不会删任何文件；取消勾选即可全部显示
- **右键任意缩略图 → 屏蔽此尺寸**：把当前这张图的尺寸加入黑名单，下次解析直接过滤。这也是「过滤」第三条规则的来源，用几次就越用越准。**屏蔽可在「历史」页底部撤销**（面板列出分类、尺寸与命中次数，支持逐条恢复 / 全部恢复；撤销后当前页面里被它拦下的图会立刻放回来并勾上）
- **解析可以中途取消**：长文章解析到一半想停，再点一次按钮（此时它显示为「取消解析」）就行，已经下载下来的图会保留，照样能勾选保存
- 顶部可切换排序（默认顺序 / 名称 / 尺寸 / 分辨率），「全选」按钮一键批量勾选/取消（文案随状态切换）
- 被「过滤小图与装饰」隐藏的图不会凭空消失：缩略图条底部显示「已隐藏 N 张 · 点此显示」，点开确认、再点隐藏回去；重复图与被过滤的小图在卡片角上有文字角标
- 输入框里直接按 **Enter** 也能解析（Ctrl+Enter 同效）；粘贴 HTML 源码请用「粘贴HTML」按钮

### 3. 下载

选好保存格式，点「下载选中图片」。**不用再选目录** —— 图会自动存进图库里的一个专属文件夹：

```
图片\ImgSnagWeChat\
├─ 2026-09-18_164633_秋天的第一杯奶茶\     ← 这一次下载的 8 张图
│   ├─ img_01.jpg
│   ├─ img_02.png
│   └─ …
└─ 2026-09-18_171205_城市黄昏随拍\         ← 另一篇文章，各归各的
```

- 文件夹名 = `日期_时分秒_文章标题`，按名字排序就是时间顺序；同一秒重复下载会自动加 `_2`
- 标题取不到（或含非法字符被清空）时用「微信图片」兜底，绝不拿空名字去建目录
- 想把**这一次**放到别处：状态栏右下角「另存到…」（仍会新建一个专属子文件夹）
- 想看看图都存哪了：状态栏右下角「打开图库」，优先打开最近一次落盘的那个文件夹

状态栏会显示进度与结果统计（共几张、几张有效、几张重复）。写盘与格式转换在后台进行，
图多时界面不会卡住，按钮上会显示「保存中 3/20」这样的进度。

### 4. 历史

「历史」页记录每次解析与下载（已解析 / 已下载 / 部分成功 / 失败），每条都能**再次解析**、**打开保存目录**、**删除**，也可以一键清空。「保存路径」一列显示的是那一次的批次文件夹名（日期_时分秒_标题），完整路径放悬停提示里。

本地文件夹被清理或搬走时，这一行会标注**「本地已删除」**并把「打开」按钮置灰 —— 点了没反应只会让人以为是程序坏了，直接说出来才好判断。

页面底部是**已屏蔽的尺寸**面板：列出每条屏蔽的类别（UI 装饰 / 头像 / 封面横幅）、尺寸、已过滤张数与加入时间。误屏蔽了点「恢复」即可撤销 —— 当前页面里被这条规则拦下的图会当场放回来并勾上；想一次清干净就点「全部恢复」。

## 常见问题

**Q：解析到 0 张图？**
先勾选「补充Script扫描」重新解析；仍为空的话，多半是文章需要登录/验证才能查看，或文章里确实没有图片。

**Q：下载的图还是只有几百 KB？**
先确认「原图画质」是勾选的。微信对同一张图提供多个画质档位，正文 HTML 里给的一般是压缩档（地址里的 `/640`），本程序会自动改写成原图档（`/0`）。

修复前后差多少，用一篇真实的 8 图文章实测（`tools/e2e_article_download.py`）：

| | 旧版（`/640`） | 当前版本（`/0`） | 提升 |
|---|---|---|---|
| 单图体积 | 93.4 – 175.3 KB | 363.8 – 748.9 KB | 3.17 – 4.29 倍 |
| 单图分辨率 | 1080×1440 | 2155×2873 ~ 2560×3413 | 3.98 – 5.62 倍像素 |
| **全篇 8 张合计** | **1131 KB（约 1.1 MB）** | **4372 KB（约 4.4 MB）** | **3.86 倍** |

这就是"ImgSnag 只下来几百 KB、手动保存却有好几 MB"的全部原因——不是微信不给原图，是旧版从来没要过原图档。

`/0` 已经**探明是天花板**，没有更高的档位可挖：

- `/1080`、`/1280`、`/2000`、`/3000` 等档位一律被 CDN 拒绝（HTTP 400），合法档位只有 `/0` 和 `/640`
- 去掉尺寸段等同于 `/0`
- 把 CDN 前缀换成 `mmbiz` / `mmbiz_png` 等，只会拿到 0.2 KB 的"未经允许不可引用"占位图（这种垃圾小图会被程序按最小体积阈值滤掉）

所以如果换成原图档后体积仍然不大，那就是微信对**这张图本身**只提供到该画质——`/0` 就是上限，没有更高的档位可挖了。

顺带澄清一个容易误判的点：`picture_page_info_list` 里每个 item 有两条 `cdn_url`，是**两个分别上传的文件**（文件 ID 不同），一条无水印、一条带水印。实测两者的 `/0` 档体积基本相当，差值绝大多数在 2% 以内（8 图文章里水印版 4 张略大 0.4%–1.8%、4 张略小；另一篇 5 图文章里最大的一张水印版大 13%）。也就是说**并不存在"几 MB 的带水印原图"**——取无水印版不会损失画质，换带水印版也换不来更大的图。本项目只取无水印版是纯收益，没有任何画质代价。

想实测某篇文章差多少，或者自己复核上面的数字：

```bash
.venv\Scripts\python.exe tools\e2e_article_download.py <文章链接>          # 端到端跑生产链路，新旧画质对比
.venv\Scripts\python.exe tools\probe_wechat_quality.py <文章链接>          # 只测档位差距，不落盘
.venv\Scripts\python.exe tools\probe_max_quality.py <图片链接或文章链接>    # 穷举写法找画质上限
```

**Q：保存的图会不会把我上次下的覆盖掉？**
不会。v1.7.0 起每次下载都进**自己的批次文件夹**（`日期_时分秒_标题`），同秒重名会自动加 `_2`；同一文件夹内的同名文件则加 `_1` / `_2` 后缀。连下多少轮都不会丢文件。

**Q：为什么图不在「下载」文件夹里了？**
刻意改的。「下载」是使用最频繁、也最常被顺手清空的目录，图集放那里迟早被一起删掉。现在统一进 `图片\ImgSnagWeChat\`，历史里的「打开」只要文件夹还在就永远能用。想改位置或临时放别处，用状态栏的「另存到…」。

**Q：历史里的「打开」是灰的？**
那一次下载的本地文件夹已经不存在了（被清理或搬走了）。程序不会凭空恢复文件，只会如实告诉你 —— 把文件夹放回原路径即可恢复。

**Q：导出的 `img_01.jpg` 是正文第一张吗？**
是。输出顺序按正文顺序（合并 ppi 数组顺序与正文标签出现顺序），所以连号就是阅读顺序，下漫画或长图不用再手动排。

**Q：能下载别的网站的图片吗？**
不能，这是刻意设计的。贴非 `mp.weixin.qq.com` 的链接会直接提示不支持。理由见[开头](#为什么只支持微信公众号)。

**Q：下载的图有水印吗？**
微信**正文图**本身不带水印，本项目还会优先取 `picture_page_info_list` 里的无水印原图。若混进带水印的**封面图**，在缩略图上右键「屏蔽此尺寸」把它拦掉即可，下次解析同类尺寸会自动过滤。

**Q：安装时提示「错误 5：拒绝访问」？**
少数受管控的公司电脑会拦 Temp 目录写入。右键「以管理员身份运行」，或直接改用**便携版**（不写临时目录、无需管理员）。

**Q：卸载会删掉我的历史记录吗？**
不会。用户数据在用户目录，卸载程序不碰它，覆盖升级也不会丢。

**Q：点「检查更新」提示连不上 GitHub？**
`api.github.com` 在国内网络下经常连不上，公司网络更常直接限制访问 —— 这不影响程序的其他功能，换成手动下载新版本即可（每次发布都会附上安装包与便携包）。启动时那次自动检查也是静默的：连不上就什么都不说，不会弹窗打扰你。

**Q：装了加速器/公司网络能上网，为什么还是提示连不上？**
v1.8.2 起这种情况已修复。原因是这类网络会做 TLS 转发（用自己装的根证书重签流量），而 Python 的 `requests` 只信任自带的 certifi 证书列表，于是「证书没被信任」被误报成「网络不通」。程序现在会把 **Windows 证书存储里的根证书合并进 CA 包**再去请求（本机实测：对端证书由加速器的根证书签发，合并后 1.2 秒返回正常）。若仍失败，提示也会明确说是证书问题，而不是笼统说网络不通。

## 打包发版

```bash
# 双击 build.bat 也行（venv 不存在会自动创建并装依赖）
.venv\Scripts\python.exe build_release.py
```

一条命令跑完：**PyInstaller onedir → Inno Setup 安装包 → 便携 zip（LZMA）**，产物在 `installer/` 与 `dist/`。

- 采用 **onedir** 而非 onefile：不再往 `%TEMP%` 解压，启动更快、杀软误报更少，也不怕公司电脑 Temp 目录被拦
- 版本号**唯一手写在 `version.py`**，构建脚本自动同步到 `.iss`；脚本内置闸门，格式不合法或该号已打过包都会中断打包
- `build.bat` 支持传参，`--skip-build` 可跳过 PyInstaller 只重做安装包

## 项目结构

```
ImgSnag-WeChat/
├── main.py                    # 入口：QApplication + 主窗口
├── version.py                 # 版本号唯一来源（发版脚本会同步到 .iss）
├── web_image_dl/
│   ├── app.py                 # 主窗口：解析页 + 历史页，侧边栏切换
│   ├── extractor.py           # 微信图片提取器（本项目核心）
│   ├── worker.py              # QThread 下载线程 + 图片元信息
│   ├── widgets.py             # 缩略图卡片 / 大图预览 / 侧边栏按钮
│   ├── history_manager.py     # SQLite 下载历史
│   ├── blocked_config.py      # 屏蔽尺寸配置（JSON 原子读写 + 判定规则唯一来源）
│   ├── settings.py            # 界面偏好持久化（QSettings 键名唯一手写处）
│   ├── file_utils.py          # 输出文件名工具（同名自动加序号，零依赖便于单测）
│   ├── naming.py              # 图库文件夹命名（日期_时分秒_标题，清洗非法字符/保留名/超长路径）
│   ├── library.py             # 历史批次文件夹扫描（自然序/图片判定/标题反推，纯逻辑）
│   ├── updater.py             # 版本检测（问 GitHub Releases，纯逻辑可离线测）
│   └── save_worker.py         # 落盘线程：写盘 + 格式转换，避免大图集时冻界面
├── tests/
│   ├── run_all.py             # 一次跑完全部测试（发版闸门，逐个文件起独立进程）
│   ├── test_extractor.py      # 提取器与画质策略回归测试（116 项，纯逻辑，含文章标题提取）
│   ├── test_filter_rules.py   # 屏蔽尺寸与过滤规则回归测试（63 项，纯逻辑）
│   ├── test_naming.py         # 图库文件夹命名回归测试（162 项，纯逻辑：非法字符/保留名/超长/撞名）
│   ├── test_library.py        # 历史批次扫描回归测试（56 项，纯逻辑：自然序/容错/上限/标题反推）
│   ├── test_updater.py        # 版本检测回归测试（120 项，纯逻辑；UP-7 走真实的 fetch_latest_release，UP-8 合并 CA 包）
│   ├── test_history.py        # 下载历史回归测试（21 项，纯逻辑、临时 db：重新解析不抹掉下载记录）
│   └── test_ui_smoke.py       # 界面回归测试（312 项，离屏跑，含保存落盘端到端、专属文件夹、历史批次浏览、缩放/拖动、屏蔽撤销、设置记忆、关于与更新）
├── tools/                     # 诊断脚本（排查微信改版用，不参与打包）
│   ├── e2e_article_download.py     # 端到端跑生产下载链路，对比新旧画质（推荐先用这个）
│   ├── probe_wechat_quality.py     # 量化某篇文章压缩档 vs 原图的差距
│   ├── probe_max_quality.py        # 穷举地址写法，确认 /0 是否画质上限（含分辨率）
│   ├── inspect_article_html.py     # 统计文章里图片地址的尺寸段与参数分布
│   ├── inspect_picture_page_info.py# 摊开 picture_page_info_list 原文
│   ├── inspect_ppi_items.py        # 对比每个 item 的无水印版与水印版
│   └── ui_screenshots.py           # 离屏渲染界面各状态成 PNG（UI 评审/回归辅助，不参与打包）
├── ImgSnagWeChat.spec         # PyInstaller onedir 配置
├── ImgSnagWeChat.iss          # Inno Setup 安装包脚本
├── build_release.py           # 一键发版
└── build.bat                  # 双击发版入口
```

### 提取逻辑简述

`extractor.py` 是全部价值所在，四种来源合并后统一去重、再提升到原图档：

1. `picture_page_info_list` JS 变量 → 取每个 item 的第一条 `cdn_url`（**无水印版**；同 item 内嵌套的 `watermark_info.cdn_url` 是带水印的另一次上传，文件 ID 都不同）
2. `<img data-src>` → 正文图懒加载地址
3. `<img src>` → 兼容旧文章与封面
4. 可选：全量扫描 `<script>` 内的 `mmbiz.qpic.cn` 链接

然后依次做四件事：洗掉 JS/HTML 转义残留（`\x26amp;amp;`）与 `#imgIndex` 锚点 → 剔除 `round_head_img`（作者头像）→ 按图片身份（URL 里的 FILEID）合并 CDN 变体 → **改写尺寸段为 `/0` 取原图**。

尺寸档位是画质的关键：路径里的 `/640` 不是"宽 640 像素"，而是微信的一个**压缩档位**（实测 640 档给到 1080×1423），`/0` 才是原图档（同图 1280×1687）。这是"下载下来只有几百 KB"的根因。

## 致谢

### 开源库

- GUI 框架：[PySide6](https://wiki.qt.io/Qt_for_Python)（Essentials）
- HTTP 请求：[requests](https://github.com/psf/requests)
- 打包工具：[PyInstaller](https://github.com/pyinstaller/pyinstaller)
- 安装包制作：[Inno Setup](https://jrsoftware.org/isinfo.php)

### 参与开发

欢迎提交 Issue 和 Pull Request。改动提取逻辑（`extractor.py`）时，请一并说明对应的微信页面结构特征，方便回归验证。

改动后先跑回归测试（七个文件、850 项，离屏无需显示器）：

```bash
.venv\Scripts\python.exe tests\run_all.py
```

发现某篇文章解析结果不对时，用 `tools/` 里的诊断脚本先看清页面结构再动手，别凭猜测改正则。

## 更新日志

> 唯一版本记录。用户向说明见 [GitHub Releases](https://github.com/virmuran/ImgSnag-WeChat/releases)。

### v1.8.2 (2026-09-23)

修一个「网络明明是通的却报连不上」的证书问题（用户实测：同网络下 ChemCal 能更新、ImgSnag 不能）。

**根因（本机实测证据）**：用户机器上装了加速器，它对流量做 TLS 转发、用自己装的
`SteamTools Certificate` 根证书重签证书；这张根证书装在 **Windows 证书存储**里，于是：

| 客户端 | 证书来源 | 结果 |
|---|---|---|
| `urllib`（ChemCal 用的） | Windows 证书存储 | ✅ 1.3s 拿到 Release |
| `requests`（ImgSnag 用的） | 只认 certifi 自带的 CA 列表 | ❌ `CERTIFICATE_VERIFY_FAILED: unable to get local issuer certificate` |

界面把「证书没被信任」翻译成了「无法连接 GitHub，请检查网络」—— 文案把用户引向错误的方向。

- 🔐 **`system_ca_bundle()`：把系统证书存储合并进 CA 包**（`updater.py`）：certifi + Windows 的 ROOT/CA，写成 PEM 后作为 `verify=` 传给 requests；缓存到 `~/.imgsnag_wechat/ca_bundle.pem`（7 天过期、临时文件 + `os.replace` 原子写）；**非 Windows 或一张证书都读不到时返回 None**，退回 requests 默认行为 —— 半个包比没有包更危险。任何异常都不抛（检查更新是锦上添花，不该崩界面）
- 🧯 **证书失败单独给一句准确的话**：不再笼统说「无法连接」，而是点明「这条网络链路在用自签名证书转发流量（公司网络或加速器常见）」，并提示关掉加速器/代理再试 —— 用户该做的动作完全不同
- ✅ 修复后真实联网验证：`ok=True, current=1.8.1, latest=1.8.1, has_update=False`，CA 包 292 KB / 159 张证书，耗时 1.2s
- ✅ 测试：`tests/test_updater.py` 98 → **120 项**（UP-8 覆盖构建/缓存命中不重建/过期重建/枚举失败→None/读不到证书→None/无 `enum_certificates`→None/自动建目录；UP-9 证书错误文案；UP-7 加断言 `verify=` 用的确实是合并包，且拿不到包时**不许传**）；七个文件合计 **850 项**
- 🧪 反向验证：「不传 verify=」→ 抓到；「证书失败退回模糊提示」→ 抓到；「CA 包不缓存」→ 抓到

### v1.8.1 (2026-09-21)

两个用户实测报出的 bug：

- 🐛 **「检查更新」必报错**：`check_for_updates` 按注入契约 `fetch(url, timeout)` 两参调用取数函数，但真联网用的 `fetch_latest_release` 只收 `timeout` —— 点「检查更新」就报 `takes from 0 to 1 positional arguments but 2 were given`。之前的测试全用注入的假 fetch（按两参写），真联网这条路径**从没被走到过**。修复为签名一致，并新增 UP-7：mock 掉 requests 层走**真实的** `fetch_latest_release`，这类契约漂移再也漏不掉
- 🐛 **重新解析已下载的链接，下载记录凭空消失**：`history_manager.add` 是「先删后插」—— 同一 URL 重新解析时，done 那行连同保存路径被整行删掉，换成一条「已解析、0 下载」。修复为：**已有下载（done/partial 且有路径）时只刷新解析时间/图片数/备注，下载状态、张数、路径原样保留**（图还在磁盘上，记录就该还在）；没有下载记录的照旧覆盖。解析失败（文章被删是常态）同样毁不掉下载记录
- 🧰 `tests/run_all.py` 修掉一个编码坑：stdout 里有 `✗/✅` 符号，被别的工具以 GBK 管道调起时 print 报告直接 `UnicodeEncodeError` 崩掉，连 `_last_run.txt` 都来不及写（表现为闸门 rc=1 但报告是上一轮旧内容）—— stdout/stderr 强制 UTF-8
- ✅ 测试：新增 `tests/test_history.py`（**21 项**，纯逻辑、临时 db）；`tests/test_updater.py` 91 → **98 项**（UP-7）；`tests/test_ui_smoke.py` 301 → **312 项**（UI-27：真实 HistoryManager 走解析完成/解析失败回调，断言记录存活、「查看」仍可用）；七个文件合计 **828 项**
- 🧪 反向验证：「add 退回先删后插」→ test_history + UI-27 **抓到**；「fetch 签名退回单参」→ UP-7 **抓到**

### v1.8.0 (2026-09-21)

历史里已经下载的图，现在能**在软件里直接翻看**（以前只剩「打开文件夹」这一步跳资源管理器）。

- 👀 **历史行新增「查看」（双击整行同效）**：把该批次文件夹里的图读回来，进解析页显示缩略图条 + 大图预览，缩放 / 拖动 / 排序 / 筛选小图都和刚解析完一样能用；顺带把链接填回输入框，想重新解析点一下「解析」即可
- 🔒 **只读浏览**：「这些图已经在本地了」——再点一次「下载」只会在图库里堆一份同名副本（用户以为在重新下载），所以浏览期间下载按钮**禁用**并写明原因，要复制到别处走「另存到…」。`_on_download` 里还留了第二道守卫，防止绕过按钮的调用路径
- 📄 **显示磁盘上的真实文件名**：不再按序号拼 `img_XX`。旧图库里用户可能删过或改过名字，按序号显示会张冠李戴（`ImageInfo.name_hint`）
- 🔢 **自然序不是字典序**：`img_10` 必须排在 `img_2` 后面（字典序会反过来）。顺序规则独立在 `library.natural_key` 里，可单测
- 🧯 **翻旧图不许崩**：目录不存在 / 不可读 → 空列表；扩展名像图片但内容是垃圾的文件 → 跳过并在状态栏计数说明；空目录 → 弹提示说清「这里没有图片」而不是白屏；单次浏览有张数（500）与总字节（200MB）上限，手滑选中一个塞满文件的大目录也不会把内存吃光
- 🏷 **标题从文件夹名反推**：`2026-09-21_100000_历史文章` → 「历史文章」，否则「另存到…」会把日期前缀当标题再拼一层
- ✅ 测试：新增 `tests/test_library.py`（**56 项**，纯逻辑：扩展名判定、自然序、容错、上限、标题反推）；`tests/test_ui_smoke.py` 276 → **301 项**（新增 UI-26：真造一个批次文件夹→点「查看」→断言缩略图数/预览有内容/真实文件名/宽高真读出来了/下载被禁用且二次守卫拦得住/坏文件跳过/缺目录与空目录的处理）；六个文件合计 **789 项**
- 🧪 反向验证（拆掉守卫确认测试真的会失败）：「浏览时不禁用下载」→ **3 项失败**；「_on_download 二次守卫」→ **2 项失败**；「不传 name_hint」→ **1 项失败**；「空目录不弹提示」→ **1 项失败**

### v1.7.1 (2026-09-19)

两处交互调整（用户实测 v1.7.0 后的直接反馈）：

- ↩️ **「打开图库 / 另存到…」从状态栏挪回动作栏**，从左到右依次是「打开图库 → 另存到… → 下载选中图片」。v1.7.0 把它们放状态栏右侧是怕挤占动作栏宽度，实测发现**放下面根本注意不到** —— 按钮再省地方，看不见就等于没有。为此把下载按钮 padding 20→12、两个次级按钮与下拉框 padding 同步收紧，窗口最小宽度压回 768px（仍在小屏半屏可用范围）
- 🖱 **历史页「解析」点一下直接开解析**：不再只是把链接填回去、让用户回解析页再按一次「解析图片」。已有解析在跑时只填链接不触发（解析按钮的同款语义是「取消」，直接调会把正在跑的解析打断），状态栏说明原因
- 🛡 **保存进行中点「另存到…」会被拦下**：按钮挪到明面上之后保存期间仍可见可点，同时开两个落盘线程会互相覆盖进度回调，现在等这一批完成再另存
- ✅ 测试：`tests/test_ui_smoke.py` 265 → **276 项**（新增 UI-25：按钮从左到右的顺序与同行断言、「解析」直接开解析、解析中不触发、保存中「另存到…」被拦、空链接不动）；五个文件合计 **708 项**
- 🧪 反向验证：「按钮挪回状态栏」→ UI-25 当场失败（属性缺失抛错）；「解析退回只填链接」→ **3 项失败**；「撤掉保存中守卫」→ **2 项失败**
- 📐 **几何测试再去一次脆弱断言**：离屏平台的 `restoreGeometry` 在贴近屏幕右缘时会加窗框边距再钳制（实测存 780/790 都恢复成 792），宽度最后几像素不可靠 —— 宽度断言改为「来自存档且落在 [最小宽度, 屏宽]」，高度仍精确往返。另修掉一个潜伏的测试环境问题：测试文件现在自带 `QT_QPA_FONTDIR`，缺字体时控件度量漂移曾导致几何用例随机挂

### v1.7.0 (2026-09-18)

下载体验重做：**不再每次选目录，也不再把图堆进「下载」文件夹**。

- 🗂 **每次下载一个专属文件夹**：点「下载」直接落进 `图片\ImgSnagWeChat\<日期_时分秒_文章标题>\`，不再弹目录选择框。以前是平铺命名（`img_01`、`img_02`…）直接扔进所选目录 —— 一直存「下载」的话，第二篇文章的 `img_01` 就变成 `img_01_1`，越用越乱，也看不出哪张属于哪篇
- 🚫 **刻意离开「下载」目录**：那是最常被顺手清空的地方，图集躺在里面迟早跟着被删，历史里的「打开」也就断了。图库独立出来之后，只要你自己不删，历史就一直打得开（Wallpaper Engine 的「专属图库」思路 + 每批一个子文件夹的分组 —— 单用一个扁平大库会疯狂撞名，因为 Wallpaper Engine 的单位是一张壁纸且有数据库索引，我们的单位是「一篇文章的一批图」）
- 🏷 **文件夹名 = `日期_时分秒_文章标题`**：日期在前，按名字排序就是时间顺序；带时分秒，同一天下好几篇也不撞车；标题一眼看出是哪篇。取不到标题时用「微信图片」兜底，**绝不拿空名字去建目录**
- 🧼 **标题要过 Windows 文件系统的七道关**：非法字符 `\ / : * ? " < > |` 删掉；换行/制表**换成空格再压**（直接删会把「空格\n换行」粘成一个词）；点与空格交替的尾巴（`'… . . '`）要一次剥干净 —— 只 `rstrip('.')` 会留下一个尾空格，名字照样非法；`CON/PRN/AUX/NUL/COM1-9/LPT1-9` 加前缀；标题截到 24 字，并**按父目录长度反推预算**，保证整条路径不超 Windows 260 上限（超了不是报错而是「写不进去」）
- 🔁 **同秒重名自动加 `_2`**：绝不覆盖上一次下载的内容（与 `file_utils.unique_path` 同一策略）
- 📌 **文章标题是新能力**：`extractor.extract_title()` 按 `og:title` → `var msg_title` → `<title>` 依次取，还原 HTML 实体与 JS 转义（微信会双重转义）；`worker` 在解析时把它带出来给命名用。抓不到标题不报错，只是文件夹名退化成「微信图片」—— 这种**静默退化**最难发现，所以三种来源都有测试钉住
- 📍 **「另存到…」与「打开图库」放状态栏右侧**：前者这一次放到别处（仍建专属子文件夹），后者优先打开最近落盘的文件夹。**为什么不放动作栏**：动作栏已排满，再塞一个约 74px 的按钮会把窗口最小宽度从 726px 顶到 800px —— 正好是离屏测试屏的宽度，也是小屏笔记本半屏的宽度，等于让窗口没法贴边对半放。状态栏跨整行，放这里一点宽度不占
- 🛡 **图库不可用时回退，不让下载白费**：盘符没了、没写权限、路径被删 → 自动退回「手动选一个目录直接放」，并在状态栏说明原因
- 🪦 **历史页标记「本地已删除」**：文件夹被清理或搬走时，那一行标注出来并把「打开」置灰 —— 点了没反应只会让人以为程序坏了，直接说出来才好判断
- ✅ 测试：新增 `tests/test_naming.py`（**162 项**，纯逻辑：非法字符、控制字符换空格、结尾点/空格、保留设备名、长度截断、空值兜底、路径预算逐段验证、撞名 `_2`/`_3`、以及「一堆难缠标题过一遍结果必须条条合法」的属性检查）；`tests/test_extractor.py` 94 → **116 项**（新增标题提取：三种来源、优先级、双重转义还原、取不到返回空串）；`tests/test_ui_smoke.py` 235 → **265 项**（新增 UI-23 专属文件夹端到端 / UI-24 本地已删除标记；UI-8 改为验证「真的落进自动建出来的子文件夹」），五个文件合计 **697 项**
- 🧪 反向验证（拆掉守卫确认测试真的会失败）：改回「每次都弹选择框」→ **13 项失败**；「永远认为文件还在」→ **4 项失败**；「撞名直接返回同名路径」→ naming 与 UI 共 **3 项失败**；「结尾点/空格不清理」→ **15 项失败**
- 📐 **顺带修正几何测试**：原断言写死 `resize(720)` 后宽应为 720，但窗口有布局算出来的最小宽度（约 728px），一旦动作栏变化就会把「被顶回几像素」误报成「没记住」。现改为记录**实际达成的尺寸**再对比，测试不再和布局较劲

### v1.6.0 (2026-09-18)

补上「关于」与「检查更新」—— 此前用户想知道有没有新版，只能自己去 GitHub 看：

- 🔔 **关于对话框**：侧边栏底部的版本号现在可以点，打开「关于」：版本、源码地址、**你的数据分别存在哪**（下载历史 / 屏蔽规则 / 界面偏好三处，用户要找回自己的文件时能照着找）、第三方依赖与许可、免责声明
- 🔍 **检查更新**：`web_image_dl/updater.py` 只做一件事 —— 问 GitHub「最新发布的 tag 是多少」，与本地 `version.py` 比一比。**只提示、不自动替换**：本项目的安装包与便携包是两种用法（安装 vs 解压），自动挑一个下载反而容易挑错形态，所以检出后引导用户去 Release 页面自选
- 🤫 **静默检查不打扰**：启动后延迟 2 秒自动查一次（可在「关于」里关掉），发现新版本**只在侧边栏亮一个「⬆ vX.X.X」**，不弹窗；连不上 GitHub 时一声不吭 —— 公司网络连不上 `api.github.com` 是常态，每次启动都弹窗只会让人烦
- ⏱ **6 小时节流**：GitHub 未认证请求按 IP 限流 60 次/小时，每次启动都查迟早把配额耗光、之后全变 403。记一个时间戳，6 小时内不重复请求
- 🧾 **版本号只认 tag（从 ChemCal 学到的教训）**：发版时若 tag 写 `v1.5.0` 而资产文件名写成 `1.6.0`，客户端只读 tag 就会永远认为「已是最新」，更新通道静默失效且不报任何错。所以把 tag、资产名、两者是否一致一并记下来，手动检查时直接提示「这个 Release 的文件名版本号和 tag 对不上」
- 🛡 **检查更新不许把界面搞崩**：网络超时、HTTP 403/404/5xx、返回结构被代理换掉…… 全部转成人话写进 `UpdateInfo.error`，`check_for_updates()` 承诺**任何失败都不抛异常**（这条由测试逐条钉住）。检查跑在后台线程，再慢也不冻界面
- 📋 **更新对话框**：写明当前版本 / 新版本 / 本次发布有哪些文件（安装包 29.1 MB · 便携包 30.4 MB）+ 更新说明原文，按钮是「稍后」与「前往下载」
- 🙈 **隐私说明说实话**：以前宣传「纯本地运行」，但检查更新要访问 GitHub 接口。现在如实写明「唯一的对外请求是检查更新」并给出关闭入口 —— 自查更新起，这个程序确实会联网了，不该再含糊
- ✅ 测试：新增 `tests/test_updater.py`（**91 项**，纯逻辑、**不联网**：网络入口全部用注入的假数据覆盖 tag 解析与逐段比较、资产识别与排序、体积格式化、403/404/5xx/超时/连接失败/返回结构异常、tag 与资产名不一致、以及「错误描述函数自己不许抛第二次」）；`tests/test_ui_smoke.py` 扩充到 **235 项**（新增 UI-22：版本号入口 → 关于对话框内容、自动检查开关写设置、6 小时节流、真实线程跑一遍检查、更新对话框，以及「静默一律不弹窗 / 失败必须说话 / 已是最新要清掉提示」三条分支），四个文件合计 **483 项**
- 🧪 反向验证：拆掉「6 小时节流」与「已是最新清提示」→ **5 项按预期失败**；拆掉「静默不弹窗」→ 启动 2 秒后的自动检查会弹出真实模态框、测试当场卡死（正说明这条守卫是必需的）
- 🧪 让测试真的不联网：`_isolated_config` 顺手把「上次检查时间」设成当前时刻，用节流挡住窗口构造后那次自动检查

### v1.5.0 (2026-09-18)

两件事：把「屏蔽尺寸」从单向门改成双向门 + 让界面记事。

- 🚪 **屏蔽尺寸不再只进不出**：旧版只有 `add_blocked` 一个入口 —— 右键误屏蔽一个尺寸，那类图从此**静默消失**，看不到屏蔽了哪些尺寸，也没有任何撤销入口，只能去手改 `%APPDATA%\ImgSnagWeChat\blocked_sizes.json`。对普通用户而言等于「图丢了」。现在「历史」页底部有**已屏蔽的尺寸**面板：按分类列出尺寸、已过滤张数、加入时间，逐条「恢复」或一键「全部恢复」
- ↩️ **撤销立刻看得见**：点「恢复」后，当前页面里被这条规则拦下的图**当场放回来并勾上**（撤销屏蔽的本意就是「这些我要下」，不该让用户再一张张点一遍）。撤销后仍该藏起来的（比如它本来就够小）会继续藏着 —— 判定走同一份规则，界面和解析结果不会说两套话
- 🏷 **角标区分来源**：被屏蔽的图角标写「**已屏蔽**」，启发式拦下的仍写「小图」，tooltip 逐条说明是被哪条规则拦下的 —— 用户才知道原来还能去撤销
- 🔒 **配置写入改为原子替换**：原来是直接覆盖写原文件，进程若在写的中途被杀掉，留下的是半截 JSON，下次启动会当损坏文件、连带攒下的规则一起丢掉。现在先写 `.tmp` + fsync 再 `os.replace`；真损坏了也会把原文件留成 `.corrupt` 供人工抢救
- 📊 **命中统计**：解析结束时批量回写每条规则拦下了多少张（同一尺寸常命中几十张，逐张写盘会把解析拖成磁盘 IO 表演）
- 🧵 **线程安全**：解析在后台线程回写统计，此刻用户可能正在界面里撤销 —— 读写套同一把可重入锁
- 💾 **界面开始记事**：此前全项目零 `QSettings`，窗口大小位置、排序方式、保存格式、两个过滤开关、上次保存目录每次启动都回到默认。现统一由 `web_image_dl/settings.py` 记住（键名与默认值只写一份，写错不会被报错、只会表现为「设置没记住」，是最难查的一类 bug）
- 📂 **保存目录从上次的地方开始**：以前每次都从「文档」开始，一套图集存多次就得反复导航；记下的目录若已被删除则回落到系统默认，不会弹一个不存在的路径
- 🔧 **判定规则只写一份**：三条过滤规则（小图 / 透明 PNG / 用户屏蔽尺寸）原先散在 worker 的循环里，界面想重算只能抄一份。现统一为 `blocked_config.classify()`，且**返回命中的全部规则**而不是一个 bool —— 撤销屏蔽才能只撤掉其中一条
- ✅ 测试：新增 `tests/test_filter_rules.py`（**63 项**，纯逻辑：规则边界、增删清空、命中累加、原子写、老格式兼容、坏文件抢救、脏数据清洗、6 线程并发读写的计数不丢）；`tests/test_ui_smoke.py` 扩充到 **191 项**（新增屏蔽面板端到端：右键屏蔽 → 角标/tooltip/隐藏 → 面板点「恢复」→ 图放回并勾上 → 面板回空态；以及设置持久化：排序/格式/两个开关/窗口几何/保存目录/存坏值回落）；新增 `tests/run_all.py` 一次跑完全部 **348 项**（每个文件独立进程，避免 Qt 全局状态互相污染）
- 🧪 反向验证：把「恢复」与「恢复设置」两条路径临时拆掉后重跑，**31 项按预期失败**，确认用例真的走到了修复代码上

### v1.4.0 (2026-09-18)

把「直接左键拖动」扶正为主路径 —— 实测手感比按住空格好得多：

- 🖱 **空格不再是前提**：左键拖动升为唯一主路径（中键也行）。按住空格拖仍可用，但已降为备选，界面上不再教用户去按它
- ✋ **光标常驻提示**：只要图超出视口，**不按任何键**光标就是张开的手；拖动中握拳，装得下时是箭头（不许骗人）。旧版「手形」只在按住空格时才出现，等于把「能拖」这个信号藏起来了
- 💬 **几何提示也搬到左键上**：图整张装在窗口里时按下左键，右下角提示「整图已全部可见 · 先 Ctrl+滚轮放大再拖」。旧版只在按空格时提示，而用户根本不会去按空格，所以只觉得「点了没反应」
- 🎚 **空格不再改变起步门槛**：按住空格按下也是先过 4px 门槛才动，和直接左键同一条规则，双击切 100% / 单击选图都不会被带歪
- 🐛 **顺手拆掉一颗递归炸弹**：曾试过在事件过滤器里把「没走成拖动」的按下手动转发给预览控件，结果过滤器装在 viewport 与 label 两层上，转发会立刻重新进同一个过滤器 —— 26MB 的 RecursionError 堆栈、测试直接挂死。已改为交回 `super()` 正常派发
- ✅ `tests/test_ui_smoke.py` 扩充到 **149 项**：新增「直接左键拖 = 主路径」用例（不按键即手形光标、装得下时给几何提示、反复按左键不卡状态不抛异常）；另修正了 UI-16/18 中「按下即拖」的旧断言。`tests/test_extractor.py` 保持 **94 项**

### v1.3.1 (2026-09-17)

修「按住空格拖动，有时拖得动、有时拖不动」：

- 🖱 **直接左键拖动**：图片放大超出窗口后，不按空格、直接按住左键拖也能平移（中键也行）。起步有 4px 门槛，单击选图、双击切 100% 不会被带歪
- 🎯 **空格归谁改为「看指针在哪」**：旧版要求「焦点不在输入框里」才给拖——用户在链接框里点一下之后整段拖动就废了（手形光标不出来、按下去没反应，毫无提示）。现在只要指针压在预览区上，拖动就生效；指针不在预览区（在打字）时空格才原样放行
- 💬 **「没得拖」会给提示**：图整张都装在窗口里时，往哪边拖都没有被挡住的画面可以露出来，这是几何决定的。旧版一声不响，现在按空格会在右下角提示「整图已全部可见 · 先 Ctrl+滚轮放大再拖」
- 🔧 其他加固：指针位置改用 Qt 自己的 Enter/Leave 状态（不再拿屏幕坐标反算，缩放/多屏更稳）；比例指示器不再吞掉鼠标事件；松开事件落到别的控件上也能正常结束拖动（旧版会卡在拖动中、光标一直握拳）
- ✅ `tests/test_ui_smoke.py` 扩充到 **135 项**：新增「焦点在文本框里也照样能拖」（含手形光标与真实平移断言）、左键拖动的起步门槛、松开事件落到别处、以及一条走**真实事件分发路径**（QTest → 窗口系统 → 命中测试）的拖动用例。三种旧行为（焦点守卫、只认空格、没得拖时一声不响）均做过反向验证，拆掉修复后按预期失败

### v1.3.0 (2026-09-17)

- 🖱 **缩放锚点改为鼠标位置**：以前 Ctrl+滚轮永远从左上角放大，"想看某个细节"就得放大后拖着滚动条慢慢找。现在以鼠标所在位置为中心缩放——鼠标指着哪个像素，缩放前后那个像素就钉在原地，指哪放哪（实现：把鼠标位置反算成原图坐标，缩放后重设滚动条把它摆回原位）
- ✋ **空格 + 拖动平移**：图片放大到超出窗口后，按住空格（光标变手形）再按左键拖动即可平移，松开恢复。空格键是应用级事件过滤器接的，放行条件收得很紧——鼠标不在预览区、焦点在输入框里、图没超出视口这三种情况都不拦，不会影响正常打字和按钮操作
- ✅ `tests/test_ui_smoke.py` 扩充到 **118 项**：新增缩放锚点几何断言（鼠标下的原图像素缩放前后位移 ≤ 4px、横向滚动条必须被推开）、空格拖动端到端（拖 N 像素滚动条走 N 像素、没按空格拖不动、光标手形切换）、空格放行守卫（图装得下时不拦、焦点在链接框里不拦且空格真的敲得进去）；两条核心用例均做过反向验证（拆掉修复后按预期失败）

### v1.2.0 (2026-09-17)

- 🔍 **预览支持缩放**：以前预览区只会把图缩到"适应窗口"，想确认下载的是不是真原图根本没法看。现在 Ctrl+滚轮自由缩放、**双击在"适应窗口 / 100%"之间切换**、Ctrl+0 / Ctrl+1 快捷键，右下角常驻显示当前比例。适应窗口模式依旧不放大（避免无意义的插值糊化）；缩放倍率按图片尺寸设了像素预算上限，防止放大到 8 倍时单张 QPixmap 吃掉 2GB 内存
- 🏷 **缩略图卡片重排**：原来的三行小字（文件名 10px / 尺寸 9px / 体积 **8px**）合并为两行、字号整体调大一档；重复图加「重复」角标（原来只有一个几乎看不出来的淡黄边框），被过滤的小图带「小图」角标；tooltip 补全 文件名/分辨率/体积/来源（原图档还是回退压缩版）/原始 URL
- 👻 **被过滤的图不再凭空消失**：缩略图条底部出现「已隐藏 N 张小图/装饰 · 点此显示」，点一下就能看回来再决定，再点隐藏回去
- 🧭 **空状态引导**：首次打开预览区不再是一大块灰，显示三步引导（粘贴链接 → 解析 → 勾选下载）
- 🔎 **历史页搜索**：按链接 / 保存路径 / 状态过滤（中文「失败」或英文 `failed` 都能命中）；URL 列只显示尾部 ID，不再被 `mp.weixin.qq.com/s/` 前缀占满大半列宽（完整链接在 tooltip）；无记录/无匹配时给提示行
- 🧹 **操作栏瘦身**：「全选 / 取消全选」合并为一个按钮（文案随勾选状态自动切换），窗口最小宽度从实测 994px 降到 910px；侧边栏按钮加图标、底部显示版本号；顶部信息栏与底部状态栏不再显示同一句话；「粘贴链接」改名「粘贴HTML」并注明用途（链接直接在输入框粘贴即可）
- ⌨️ **输入框回车即解析**：顺手修掉一处死代码——`url_input` 上装了 eventFilter 却从未实现，现在 Enter 触发解析（Ctrl+Enter 照旧，Shift+Enter 换行）
- ✅ `tests/test_ui_smoke.py` 扩充到 **86 项**：新增缩放（100% 1:1、倍率上限保护）、卡片信息行与角标、隐藏入口、全选切换、历史搜索、空状态引导、回车解析等用例；`tests/test_extractor.py` 保持 **94 项**

### v1.1.3 (2026-09-17)

- 🐛 **修复点「下载选中图片」没有任何反应**：v1.1.2 的 `app.py` 漏掉了 `SaveWorker` 的 import（同一文件多处并行修改时被覆盖），运行时按下保存直接 `NameError`，表现是状态栏卡在「正在保存」、按钮停在「保存中 0/3」、界面不卡但一个文件都不写。现补回 import，保存链路恢复正常
- ✅ 新增界面回归测试 `tests/test_ui_smoke.py`（**36 项**，离屏运行），其中新增的保存链路用例会**真的按一次保存按钮**并等文件出现在磁盘上——上述 bug 只有走到这条路径才会暴露，因此必须有这样一条测试钉住（已做反向验证：故意删掉 import 该用例会失败并准确指出原因）
- ✅ `tests/test_extractor.py` 保持 **94 项**全通过

### v1.1.2 (2026-09-17)

- 🐛 **修复预览图越缩越糊**：窗口缩放事件里是对**已经缩放过的**预览图再缩放，拖几次窗口画质就逐级劣化且再也回不来。现改为始终保留原始像素、每次都从原图重算，并且小图不再被强行放大（放大只增加糊度、不增加信息）
- 🐛 **收紧链接校验**：原先判断"是不是公众号链接"用的是子串匹配 `'mp.weixin.qq.com' in url`，`https://evil.com/mp.weixin.qq.com/…`、`https://mp.weixin.qq.com.evil.com/…` 这类地址都会被放行并真的发起请求。现改为解析 URL 后比对主机名，端口、大小写、锚点不影响判定
- ⚡ **保存过程不再卡界面**：写盘与格式转换原在界面线程里同步跑，图一多就整窗口冻住、进度条也不动。现挪到独立线程，按钮上实时显示「保存中 3/20」
- ⚡ **解析支持中途取消**：长文章解析到一半可以再点一次按钮（显示为「取消解析」）中止，**已经下载下来的图会保留**，照样能勾选保存
- 🧹 **修正「过滤小图与装饰」的说明文案**：原来的提示里列了一串"已知噪音尺寸"（`321×192` / `272×272` / `1080×460` 等），但配置里从来没内置过这些值——文案与实现不符。现如实改为三条真正生效的规则：最长边 ≤ 200px、小于 10KB 的透明 PNG、以及你右键标记过的尺寸
- 🔧 **健壮性**：关窗时会等待后台线程收尾（避免线程仍在运行时被销毁）；格式转换失败时不再留下 0 字节垃圾文件；整批保存中单张出错只记账不中断其余
- ✅ `tests/test_extractor.py` 扩充到 **94 项**：新增链接校验（放行 6 例 / 拒绝 8 例）、保存链路（原格式零重编码、真转码、失败回退不留垃圾、连存不覆盖）、整批落盘统计、中途取消等用例

### v1.1.1 (2026-09-17)

- 🐛 **修复导出顺序错乱**（重要）：提取结果最终会被 `sorted()` 按字母序排列，而 mmbiz 地址前缀高度雷同，字母序会把正文顺序彻底打乱——实测一篇 8 图文章被排成 `8,3,4,5,6,1,7,2`，`img_01.jpg` 拿到的根本不是正文第一张，下漫画/长图/教程步骤这类连号图集只能手动重排。现改为按**正文顺序**输出：合并「`picture_page_info_list` 数组顺序」与「正文标签（`data-src` / `img src`）出现顺序」两个来源——正文覆盖完整时以正文顺序为准，否则以 ppi 数组为主序并把正文独有的图插回原处
- 🐛 **修复同名文件静默覆盖**（会丢文件）：同名时只补一次 `_count` 后缀且不校验该名字是否已被占用，同一目录下载第三次会把 `img_01_0.jpg` 直接覆盖掉。现改为循环探测 `_1` / `_2` / … 直到找到空位
- 🐛 **修复格式转换失败时的落盘路径**：旧实现用 `str.replace` 改扩展名，若目录路径里恰好含该扩展名会写到错误位置（改为重新探测文件名）
- ✅ `tests/test_extractor.py` 扩充到 **58 项**：新增输出顺序（含 ppi 漏图的混合结构、纯 ppi 结构）、同名不覆盖、序号占用探测等用例
- 🔧 新增 `web_image_dl/file_utils.py`（零依赖的文件名工具，便于单测）

### v1.1.0 (2026-09-17)

- 📐 **新增「原图画质」（默认开启）**：修正"下载下来只有几百 KB"的画质问题。根因是微信正文图地址里带画质档位——路径中的 `/640` 是压缩档、`/0` 才是原图档，旧版直接拿文章里的地址下载，拿到的一直是压缩档。现统一改写为 `/0`。用一篇真实的 8 图文章端到端实测：`1080×1440` → `2560×3413`，全篇合计 `1.1 MB` → `4.4 MB`（**3.86 倍**）
- 🛡 **原图失败自动回退**：原图档取不到（404 / 超时 / 返回过小）时自动退回压缩档，不会整张失败；状态栏会统计有多少张走了回退
- 🧹 **剥离噪声参数**：`tp=webp`（强制转有损图）、`wx_lazy`、`wxfrom`、`wx_co`、`#imgIndex` 锚点等一律清掉
- 🐛 **修复 JS/HTML 转义未清洗**：`picture_page_info_list` 里的地址原文是 `.../640?wx_fmt=gif\x26amp;amp;from=appmsg`，带转义残留会让请求拿到错误响应（现统一还原为 `&`）
- 🐛 **修复 `picture_page_info_list` 只认单引号**：真实结构确认为单引号 JS 字面量，但放宽到单/双引号都兼容，避免微信改写法后静默失效；同时确认每个 item 的两条 `cdn_url` 是**两个不同文件**（无水印版 + 带水印版，FILEID 不同），只取无水印那条
- 📊 解析完成后状态栏显示本次合计体积与回退张数
- ✅ 新增 `tests/test_extractor.py`（44 项回归测试）：地址改写、非 mmbiz 地址不误伤、CDN 变体合并、头像排除、无水印/水印版区分、转义清洗、下载回退与候选顺序
- 🔧 新增 `tools/` 诊断脚本 6 个，用于微信改版后快速定位（端到端画质对比、量化档位差距、穷举画质上限、统计地址结构、摊开 JS 变量）

### v1.0.0 (2026-09-17)

- 🎉 首个版本：从通用版 ImgSnag 收缩为**微信公众号专精版**
- `extractor.py` 只保留微信策略，移除抖音 / 微博 / 森空岛 / 小黑盒 / 堆糖 / C站等平台适配
- 移除 Playwright 与浏览器模式，改纯 requests 抓取；依赖仅剩 `PySide6-Essentials` + `requests`
- 数据目录与旧版隔离：历史库 `~/.imgsnag_wechat/`、屏蔽尺寸 `%APPDATA%/ImgSnagWeChat/`
- 链接入口增加校验：非 `mp.weixin.qq.com` 直接提示不支持，不做静默兜底
- 打包链路落地（复刻 ChemCal）：PyInstaller onedir + Inno Setup + 便携 zip，`build.bat` 一键发版

## 免责声明

本项目仅供个人学习与合法用途，用于下载你自己有权访问和保存的内容。请遵守微信平台服务条款与《著作权法》相关规定，**不要**用于批量抓取、商业转载或任何侵犯他人权益的行为。使用本工具产生的一切后果由使用者自行承担。

## 许可

本仓库**尚未声明开源许可证**。在补充许可证之前，默认保留所有权利。
